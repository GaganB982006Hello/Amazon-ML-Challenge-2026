Act as an expert Machine Learning Engineer and generate a complete, end-to-end Python codebase for the "Amazon ML Challenge 2026: Business Entity Resolution."Task Overview:
I need to match noisy business records from two data sources (Source 2 and Source 3) to a clean reference source (Source 1). The data contains entity_id, business_name, business_address, and country. S1 entities can have zero, one, or multiple matches.   Strict Rules & Constraints:No External Data: You must not use any external APIs, geocoding, or external databases.   File Formats: All input and output files are .tsv (tab-separated). You must use sep="\t" in pandas. List of matched IDs must be comma-separated.   Singletons: If an S1 entity has no match, the ID list must be completely empty (not even a space). Every test S1 entity must be present in the final output.   Metric: The evaluation metric is macro-averaged $F_{0.5}$ (formula: (1.25 * P * R) / (0.25 * P + R)). Because precision is heavily weighted, the model needs a high classification threshold (e.g., > 0.8) rather than the default 0.5.   Required Pipeline Architecture:Please generate modular Python scripts (main.py, blocking.py, features.py, model.py) and a requirements.txt that execute the following pipeline:Stage 1: Blocking / Candidate Generation (blocking.py)Do not do $O(N^2)$ comparisons. Create a fast retriever using TF-IDF character n-grams and Cosine Similarity on combined names and addresses to bucket likely matches.   Extract the top 30 candidates from S2/S3 for each S1 entity.Save these candidates to output/candidate_pairs.tsv with columns source1_entity_id and candidate_entity_id (comma-separated list of candidate IDs).   Stage 2: Feature Engineering (features.py)For every generated candidate pair, compute pairwise features: Exact match boolean, Levenshtein distance ratio, Jaro-Winkler similarity, and Token Set/Sort ratios (using rapidfuzz) for both business_name and business_address.   Add a boolean feature for exact country match. Do not hardcode country names (like US or India), as the test set contains new countries like France.   Stage 3: Modeling & Inference (model.py)Train a LightGBM or XGBoost binary classifier on the candidate pairs generated from the training sets. Label is 1 if the pair exists in train_ground_truth.tsv, 0 otherwise.   Write a function to tune the prediction probability threshold to maximize macro $F_{0.5}$ on a local validation split.Run inference on the test set. Filter predictions using the optimized threshold.Format the final filtered matches and save them to output/matching_results.tsv with columns source1_entity_id and matched_entity_ids (comma-separated, empty if no matches).   Output the exact folder structure and the complete, runnable Python code for each file. Ensure the code handles memory efficiently.
